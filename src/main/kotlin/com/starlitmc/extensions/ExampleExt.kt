package com.starlitmc.extensions

// new -> Kotlin File - name it after the class you're extending eg. ServerPlayerEntityExt
// create an extension function like below

// fun ServerPlayerEntity.offerOrCache(reward: ItemStack, expiryMinutes: Int = -1):
// RewardsGivenOrCached {
//    return giveRewardCacheRemaining(this, reward, expiryMinutes)
// }
